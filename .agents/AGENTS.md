# Workspace Rules

## Terminal Execution Visibility
- **Always disclose terminal activity**: Whenever you execute a command in the terminal on behalf of the user, you MUST explicitly state what command you ran and provide the output or a summary of the output in your chat response so the user can easily see what happened. Always Open the terminal that you are running so the User can see the terminal input and output.
